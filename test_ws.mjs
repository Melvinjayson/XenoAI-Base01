import WebSocket from "ws"; const ws = new WebSocket("ws://localhost:5000/ws"); ws.on("open", function() { console.log("Connected to WebSocket server"); ws.send(JSON.stringify({type: "status"})); }); ws.on("message", function(data) { console.log("Received:", data.toString()); setTimeout(() => ws.close(), 1000); }); ws.on("error", function(err) { console.error("Error:", err); }); ws.on("close", function() { console.log("Connection closed"); });
